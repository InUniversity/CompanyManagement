<UserControl x:Class="CompanyManagement.UserControls.TasksInProjectUC"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"    
             DataContext="{StaticResource TasksInProjectVM}"      
             TextElement.Foreground="{DynamicResource MaterialDesignBody}"
             TextElement.FontWeight="Regular"
             TextElement.FontSize="13"
             TextOptions.TextFormattingMode="Ideal"
             TextOptions.TextRenderingMode="Auto"
             x:Name="tasksInProjectUC"
             Background="{DynamicResource MaterialDesignPaper}"
             FontFamily="{DynamicResource MaterialDesignFont}"
             mc:Ignorable="d" 
             d:DesignHeight="725" d:DesignWidth="800">
    <Grid>
        <Border
                Margin="0"
                Height="auto"
                BorderBrush="{DynamicResource PrimaryHueMidBrush}"
                BorderThickness="2"
                ClipToBounds="True">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="50"></RowDefinition>
                    <RowDefinition Height="675"></RowDefinition>
                 </Grid.RowDefinitions>
                <Grid Grid.Row="0" Background="WhiteSmoke">
                    <TextBlock HorizontalAlignment="Center" VerticalAlignment="Center" Text="Danh sách công viêc" FontSize="18" FontWeight="Bold" FontStyle="Oblique"/>
                </Grid>

                <Grid Grid.Row="1">
                    <ListView ItemsSource="{Binding TasksInProject}">
                        <ListView.ItemTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock 
                                        HorizontalAlignment="Center" 
                                        VerticalAlignment="Center" 
                                        Margin="0 0 10 0"
                                        Text="{Binding Title}" 
                                        FontSize="22" />
                                    <TextBlock 
                                        HorizontalAlignment="Center" 
                                        VerticalAlignment="Center" 
                                        Margin="0 0 10 0"
                                        Text="{Binding EmployeeID}" 
                                        FontSize="22" />
                                    <TextBlock 
                                        HorizontalAlignment="Center" 
                                        VerticalAlignment="Center" 
                                        Margin="0 0 10 0"
                                        Text="{Binding AssignDate}" 
                                        FontSize="22" />
                                    <TextBlock 
                                        HorizontalAlignment="Center" 
                                        VerticalAlignment="Center" 
                                        Margin="0 0 10 0"
                                        Text="{Binding Deadline}" 
                                        FontSize="22" />
                                    <TextBlock 
                                        HorizontalAlignment="Center" 
                                        VerticalAlignment="Center" 
                                        Margin="0 0 10 0"
                                        Text="{Binding Description}" 
                                        FontSize="22" />
                                </StackPanel>
                            </DataTemplate>
                        </ListView.ItemTemplate>
                    </ListView>
                    <Button
                        Grid.Row="1"
                        Width="60"
                        Height="60"
                        Margin="30"
                        HorizontalAlignment="Right"
                        VerticalAlignment="Bottom"                       
                        ToolTip="Thêm công việc mới"      
                        Command="{Binding ElementName=tasksInProjectUC, Path=DataContext.OpenTaskInProjectInputCommand}"
                        Content="{materialDesign:PackIcon Kind = Plus , Size=30}"
                        Style="{StaticResource MaterialDesignFloatingActionButton}" />
                </Grid>

            </Grid>
        </Border>
        <DataGrid CanUserAddRows="False"
                              AutoGenerateColumns="False"
                              Background="White"
                              Height="625"
                              ItemsSource="{Binding TasksInProject}">
            <DataGrid.Columns>
                <DataGridTemplateColumn Header="Operations" CanUserResize="False" IsReadOnly="True"
                                                    Width="auto">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal">
                                <Button
                                                Command="{Binding DataContext.UpdateTaskInProjectCommand, ElementName=tasksInProjectUC}"
                                                CommandParameter="{Binding}">
                                    <materialDesign:PackIcon Kind="PencilOutline" />
                                </Button>
                                <Button Margin="5 0 0 0"
                                                    Command="{Binding DataContext.DeleteTaskInProjectCommand, ElementName=tasksInProjectUC}"
                                                    CommandParameter="{Binding ID}">
                                    <materialDesign:PackIcon Kind="DeleteOutline" />
                                </Button>
                            </StackPanel>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
                
                <DataGridTextColumn Header="Tên nhiệm vụ" IsReadOnly="True" Binding="{Binding ID}"/>
                <DataGridTextColumn Header="Tiêu đề" IsReadOnly="True" Binding="{Binding Title}" />
                <DataGridTextColumn Header="Mô tả" IsReadOnly="True" Binding="{Binding Description}" Width="200"/>
                <DataGridTextColumn Header="Ngày bắt đầu" IsReadOnly="True" Binding="{Binding AssignDate}" />
                <DataGridTextColumn Header="Ngày kết thúc" IsReadOnly="True" Binding="{Binding Deadline}" />
                <DataGridTextColumn Header="Nhân viên" IsReadOnly="True" Binding="{Binding EmployeeID}" />
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
